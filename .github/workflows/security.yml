name: Security Scan

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  schedule:
    - cron: '0 0 * * 0'  # Weekly on Sunday at midnight

permissions:
  contents: read

jobs:
  npm-audit:
    name: npm Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: Run npm audit
        run: npm audit --audit-level=high || echo "::warning::npm audit found vulnerabilities"
        continue-on-error: true

      - name: Audit web dependencies
        working-directory: ./web
        run: |
          if [ -f package.json ]; then
            npm install --ignore-scripts
            npm audit --audit-level=high || echo "::warning::web npm audit found vulnerabilities"
          fi
        continue-on-error: true

      - name: Audit frontend dependencies
        working-directory: ./apps/frontend
        run: |
          if [ -f package.json ]; then
            npm install --ignore-scripts
            npm audit --audit-level=high || echo "::warning::frontend npm audit found vulnerabilities"
          fi
        continue-on-error: true

      - name: Audit server dependencies
        working-directory: ./server
        run: |
          if [ -f package.json ]; then
            npm install --ignore-scripts
            npm audit --audit-level=high || echo "::warning::server npm audit found vulnerabilities"
          fi
        continue-on-error: true

  python-security:
    name: Python Security Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: scripts/python/requirements.txt

      - name: Install Python dependencies
        working-directory: ./scripts/python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install bandit safety

      - name: Run Bandit security linter
        working-directory: ./scripts/python
        run: bandit -r app -ll || echo "::warning::Bandit found potential security issues"
        continue-on-error: true

      - name: Run Safety check
        working-directory: ./scripts/python
        run: safety check -r requirements.txt || echo "::warning::Safety found vulnerable dependencies"
        continue-on-error: true
