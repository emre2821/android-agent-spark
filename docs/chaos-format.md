# CHAOS Format

`.chaos` files capture serialized agent instructions and demo payloads. They are used for quick smoke tests and scripted agent scenarios.

This repository includes placeholders; align the schema with your runtime before shipping.
